name: commit

on:
  pull_request:

jobs:
  test:
    uses: ./.github/workflows/_test.yml
  deploy:
    uses: ./.github/workflows/_deploy.yml
    with:
      app-name: ${{ env.APP_NAME }}
    secrets: inherit
